namespace Spinner
{
    /// <summary>
    /// テレポートゾーンの色（ペア識別用ID）
    /// 同じ色は1P側と2P側に1つずつペアで配置される
    /// </summary>
    public enum TeleportColorId
    {
        Red = 0,
        Blue = 1,
        Yellow = 2,
        Green = 3,
        Purple = 4,
    }
}
